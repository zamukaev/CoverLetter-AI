services:
  postgres:
    image: postgres:16-alpine
    container_name: coverletter-ai-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: coverletter_ai
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d coverletter_ai"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
